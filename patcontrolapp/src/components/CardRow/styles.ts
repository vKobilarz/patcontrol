import { View } from 'react-native';
import styled from 'styled-components';

export const CardRowStyle = styled(View)`
  flex-direction: row;
  justify-content: space-between;
  padding: 5px 0px;
`;
